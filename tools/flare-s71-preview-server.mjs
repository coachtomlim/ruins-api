import http from 'node:http';import {readFile,stat} from 'node:fs/promises';import path from 'node:path';import {fileURLToPath} from 'node:url';

const root=path.resolve(path.dirname(fileURLToPath(import.meta.url)),'../public'),port=Number(process.env.PORT||4171);
const types={'.html':'text/html; charset=utf-8','.mjs':'text/javascript; charset=utf-8','.js':'text/javascript; charset=utf-8','.css':'text/css; charset=utf-8','.json':'application/json; charset=utf-8','.txt':'text/plain; charset=utf-8','.png':'image/png','.webp':'image/webp'};
function localPath(requestUrl){let pathname=decodeURIComponent(new URL(requestUrl,'http://localhost').pathname);if(/^\/k\/[A-Za-z0-9_-]{4}\/?$/.test(pathname))pathname='/flare-s71/challenge.html';if(pathname.startsWith('/quick-dungeon/'))pathname=pathname.slice('/quick-dungeon'.length);if(pathname.endsWith('/'))pathname+='index.html';const file=path.resolve(root,'.'+pathname);if(file!==root&&!file.startsWith(root+path.sep))throw Error('outside public root');return file}
const server=http.createServer(async(req,res)=>{try{const file=localPath(req.url);if(!(await stat(file)).isFile())throw Error('not a file');const body=await readFile(file);res.writeHead(200,{'Content-Type':types[path.extname(file).toLowerCase()]||'application/octet-stream','Cache-Control':'no-store','X-Content-Type-Options':'nosniff'});res.end(body)}catch{res.writeHead(404,{'Content-Type':'text/plain; charset=utf-8'});res.end('Not found')}});
server.listen(port,'127.0.0.1',()=>console.log(`S7.1 preview http://127.0.0.1:${port}/quick-dungeon/flare-s71/`));
