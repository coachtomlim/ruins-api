# Engine Placeholder

This directory is reserved for the future deterministic Ruins adventure engine.

No gameplay logic is implemented here yet.

Planned module boundaries are documented in [../../docs/architecture/deterministic-engine-proposal.md](../../docs/architecture/deterministic-engine-proposal.md).

The first real engine implementation should begin only after:

- canon conflicts needed for the module are resolved or explicitly versioned,
- content schemas are validated,
- deterministic tests are written,
- API boundaries are agreed.
