{
  "name": "ruins-api",
  "version": "0.1.0",
  "private": true,
  "description": "Vercel API and migration workspace for Forgotten Ruin of the Dark Galan.",
  "type": "commonjs",
  "scripts": {
    "test": "node --test",
    "lint": "node scripts/lint.js",
    "validate:openapi": "node scripts/validate-openapi.js",
    "validate:assets": "node scripts/validate-assets.js",
    "validate:content": "node scripts/validate-content.js",
    "check": "npm run lint && npm test && npm run validate:openapi && npm run validate:assets && npm run validate:content"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {}
}
