# Test References

These files are copied from `ruins-api/test/` as reference material for Fable 5.

They use `.txt` suffixes so the legacy repo's `node --test` command does not auto-discover and execute them from this detached handoff folder.

When creating `fiends-hero-roguelike`, convert or copy the relevant assertions into that repo's own `tests/` directory and update import paths for the new structure.
