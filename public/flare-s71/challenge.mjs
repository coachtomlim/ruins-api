import {loadS3ActorPack} from '../flare-s3/actors.mjs';
import {S7_ROOMS,loadS7StockRoom,drawPreview} from './rooms.mjs';
import {loadS7PlayableRoom} from './actors.mjs';
import {Simulation} from './simulation.mjs';
import {Renderer} from './renderer.mjs';
import {applyRunnerModel,runnerSummary,monsterSummary,encounterCost,buildS7Challenge,actualHpPercent,scoreForTarget,scoreLabel,SUPPORT_IDS,TRAP_IDS} from './game.mjs';
import {decodeInviteCode,inviteCodeFromLocation,inviteSender} from './flow.mjs';
import {calibrateEncounter,estimateEncounter,difficultyCue,presetById} from './calibration.mjs';
import {startComposedHeroStance} from './hero-preview.mjs';

const $=id=>document.getElementById(id),roomSpecs=Object.values(S7_ROOMS),rooms=new Map();
let model=null,invite=null,runner=null,catalog=null,roomIndex=0,step=0,sim=null,renderer=null,playing=false,paused=false,elapsed=0,acc=0,prev=0,finishDelay=0,calibrated=null,stopHeroPreview=()=>{};
function selectedSpec(){return roomSpecs[roomIndex]}function selectedRoom(){return rooms.get(selectedSpec().id)}
function encounter(){return{enemyTypes:['enemy1','enemy2','enemy3'].map(id=>$(id).value),supportTypes:SUPPORT_IDS.filter(id=>$(id).checked),trapTypes:TRAP_IDS.filter(id=>$(id).checked)}}
function sameEncounter(a,b){return a&&b&&JSON.stringify(a.enemyTypes)===JSON.stringify(b.enemyTypes)&&JSON.stringify(a.supportTypes)===JSON.stringify(b.supportTypes)&&JSON.stringify(a.trapTypes)===JSON.stringify(b.trapTypes)}
function goto(n){step=Math.max(0,Math.min(3,n));if(step>0)stopHeroPreview();$('receiverTrack').style.transform=`translateX(-${step*100}%)`;document.querySelectorAll('.receiver-progress span').forEach((d,i)=>d.classList.toggle('active',i===Math.min(step,2)));$('topTitle').textContent=['Game invitation','Choose a dungeon','Optional customization','The run'][step];if(step===1)requestAnimationFrame(renderRoom);if(step===2)refreshBuild()}
function populateRunner(){const pct=invite.targetHp,name=inviteSender(location.search);$('sender').textContent=name;$('missionTarget').textContent=`Target ${pct}% HP`;$('missionRunner').textContent=`Level ${runner.level} ${runner.name}`;$('goalTarget').textContent=`${pct}% HP`;$('goalRunner').textContent=`Level ${runner.level} ${runner.className} · ${runner.hp} HP · ${runner.attack} ATK · ${runner.defense} DEF`;for(const [id,out] of [['goblin','goblinVs'],['skeleton','skeletonVs'],['goblin-elite','eliteVs'],['antlion','antlionVs']]){const m=monsterSummary(id,model,invite.runnerId,catalog,runner);$(out).textContent=`HP ${m.hp} · ATK ${m.attack} · DEF ${m.defense} · hits you for ${m.damageToRunner}`}}
function renderRoom(){const spec=selectedSpec(),loaded=selectedRoom();$('roomName').textContent=spec.name;$('roomTier').textContent=`Stock Flare v1.15 dungeon · Tier ${spec.tier}`;$('roomIndex').textContent=`${roomIndex+1} / ${roomSpecs.length}`;if(loaded)drawPreview($('roomPreview'),loaded.map,loaded.tiles);$('chooseRoom').disabled=!loaded}
function changeRoom(delta){roomIndex=(roomIndex+delta+roomSpecs.length)%roomSpecs.length;renderRoom();refreshBuild()}
function applyEncounter(e){['enemy1','enemy2','enemy3'].forEach((id,i)=>$(id).value=e.enemyTypes[i]||'none');for(const id of SUPPORT_IDS)$(id).checked=e.supportTypes.includes(id);for(const id of TRAP_IDS)$(id).checked=e.trapTypes.includes(id);document.querySelectorAll('[data-preset]').forEach(b=>b.classList.remove('active'))}
function preset(name){const e=presetById(name);if(!e)return;applyEncounter(e);document.querySelector(`[data-preset="${name}"]`)?.classList.add('active');refreshBuild()}
function refreshDifficulty(e){const estimate=estimateEncounter({catalog,model,runnerId:invite.runnerId,runner,encounter:e}),cue=difficultyCue(estimate.estimatedHpPercent,invite.targetHp);$('difficultyLabel').textContent=cue.label.toUpperCase();$('difficultyNote').textContent=(sameEncounter(e,calibrated?.encounter)?'Calibrated start. ':'')+cue.note;$('calibrationStrip').dataset.level=cue.id}
function refreshBuild(){if(!catalog||!runner)return;const spec=selectedSpec(),e=encounter(),used=encounterCost(catalog,e),left=(model.budget||100)-used;$('summaryRoom').textContent=spec.name;$('spent').textContent=used;$('summaryMonsters').textContent=e.enemyTypes.filter(x=>x!=='none').map(x=>catalog.enemies[x].name).join(' + ')||'None';$('summarySupport').textContent=[...e.supportTypes,...e.trapTypes].map(x=>catalog.items[x].label||catalog.items[x].name).join(' + ')||'None';$('budgetMessage').textContent=left>=0?`${left} gold remaining`:`Over budget by ${-left}`;$('budgetMessage').classList.toggle('bad',left<0);$('budgetFill').style.width=`${Math.min(100,used)}%`;$('run').disabled=left<0||!selectedRoom();refreshDifficulty(e);$('buildStatus').textContent=left>=0?`Ready to run at target ${invite.targetHp}% HP. Customization is optional.`:'Reduce the dungeon cost to 100 gold or less.'}
function hud(){if(!sim)return;$('hp').textContent=sim.hero.hp;$('gold').textContent=sim.gold;$('time').textContent=(sim.tick/60).toFixed(1)}
function begin(){if(!sim||playing)return;sim.reset();sim.start();playing=true;paused=false;elapsed=acc=finishDelay=0;prev=performance.now();renderer.camera=null;renderer.overview=false;$('resultCover').hidden=true;$('pause').disabled=false;$('pause').textContent='PAUSE';$('runStatus').textContent='Hero-Runner is finding a legal route.';hud()}
function finishRun(){const r=sim.result(),actual=actualHpPercent(r.status,r.hp,r.maxHp),score=scoreForTarget(r.status,r.hp,invite.targetHp,r.maxHp);$('resultLabel').textContent=r.status==='cleared'?'CHALLENGE COMPLETE':'RUN ENDED';$('resultTitle').textContent=r.status==='cleared'?'Gauntlet cleared':r.status==='dead'?'Runner defeated':'Route blocked';$('score').textContent=Math.round(score);$('scoreLabel').textContent=scoreLabel(score);$('resultText').textContent=`Finished at ${actual.toFixed(1)}% HP · target ${invite.targetHp}% · ${r.gold} gold · ${r.seconds.toFixed(1)}s.`;$('resultCover').hidden=false;$('pause').disabled=true;$('runStatus').textContent=r.status==='cleared'?'Now decide whether to retry or rebuild.':r.reason}
function frame(now){const dt=prev?Math.min(.1,(now-prev)/1000):0;prev=now;if(sim){if(playing&&!paused){elapsed+=dt;if(elapsed>=.35&&sim.status==='running'){acc+=dt;while(acc>=1/60&&sim.status==='running'){sim.step();acc-=1/60}hud()}if(sim.status!=='running'){finishDelay+=dt;if(finishDelay>.45){playing=false;finishRun()}}}renderer.draw(sim,elapsed,dt,matchMedia('(prefers-reduced-motion: reduce)').matches)}requestAnimationFrame(frame)}
async function runGauntlet(){try{const spec=selectedSpec(),room=selectedRoom(),e=encounter(),built=buildS7Challenge({roomId:spec.id,roomTitle:spec.name,map:room.map,catalog,targetHp:invite.targetHp,encounter:e,budget:model.budget||100});$('run').disabled=true;$('buildStatus').textContent='Loading the animated run…';const loaded=await loadS7PlayableRoom(spec.id);sim=new Simulation(loaded.map,built.challenge,catalog);renderer=new Renderer($('scene'),loaded.map,loaded.atlases);renderer.zoom=1.62;goto(3);begin()}catch(error){console.error(error);$('buildStatus').textContent=error.message;$('run').disabled=false}}

$('accept').addEventListener('click',()=>goto(1));$('prevRoom').addEventListener('click',()=>changeRoom(-1));$('nextRoom').addEventListener('click',()=>changeRoom(1));$('chooseRoom').addEventListener('click',()=>goto(2));$('customize').addEventListener('click',()=>{const opening=$('advanced').hidden;$('advanced').hidden=!opening;$('customize').textContent=opening?'HIDE OPTIONAL OPTIONS':'CUSTOMIZE DUNGEON — OPTIONAL'});for(const b of document.querySelectorAll('[data-preset]'))b.addEventListener('click',()=>preset(b.dataset.preset));for(const id of ['enemy1','enemy2','enemy3',...SUPPORT_IDS,...TRAP_IDS])$(id).addEventListener('change',refreshBuild);$('run').addEventListener('click',runGauntlet);$('pause').addEventListener('click',()=>{if(!playing)return;paused=!paused;$('pause').textContent=paused?'RESUME':'PAUSE'});$('overview').addEventListener('click',()=>{if(renderer)renderer.overview=!renderer.overview});$('retry').addEventListener('click',begin);$('rebuild').addEventListener('click',()=>{playing=false;paused=false;$('resultCover').hidden=true;goto(2);$('run').disabled=false});
let swipe=null;$('roomStage').addEventListener('pointerdown',e=>swipe={x:e.clientX,y:e.clientY,id:e.pointerId});$('roomStage').addEventListener('pointerup',e=>{if(!swipe||swipe.id!==e.pointerId)return;const dx=e.clientX-swipe.x,dy=e.clientY-swipe.y;swipe=null;if(Math.abs(dx)>42&&Math.abs(dx)>Math.abs(dy)*1.15)changeRoom(dx<0?1:-1)});new ResizeObserver(()=>{if(step===1&&selectedRoom())renderRoom()}).observe($('receiverApp'));requestAnimationFrame(frame);
(async()=>{try{const [base,game,heroPack]=await Promise.all([fetch(new URL('../flare-p0/data/catalog.json',import.meta.url),{cache:'no-cache'}).then(r=>{if(!r.ok)throw Error('Catalogue unavailable');return r.json()}),fetch(new URL('../flare-s7/data/game.json',import.meta.url),{cache:'no-cache'}).then(r=>{if(!r.ok)throw Error('S7.1 game model unavailable');return r.json()}),loadS3ActorPack()]);model=game;const code=inviteCodeFromLocation(location);invite=code?decodeInviteCode(code,model):new URLSearchParams(location.search).get('demo')==='1'?{runnerId:model.defaultRunner,targetHp:model.defaultTargetHp,codeVersion:2}:null;if(!invite)throw Error('Challenge invitation is missing');catalog=applyRunnerModel(base,model,invite.runnerId);runner=runnerSummary(model,invite.runnerId,catalog);calibrated=calibrateEncounter({catalog,model,runnerId:invite.runnerId,runner,targetHp:invite.targetHp});applyEncounter(calibrated.encounter);populateRunner();stopHeroPreview=startComposedHeroStance($('inviteHeroCanvas'),heroPack);await Promise.all(roomSpecs.map(async spec=>rooms.set(spec.id,await loadS7StockRoom(spec.id))));renderRoom();refreshBuild();$('accept').disabled=false;$('introStatus').textContent='Your Hero-Runner is ready.'}catch(error){console.error(error);$('inviteTitle').textContent='This challenge cannot open';$('introStatus').textContent=error.message}})();
