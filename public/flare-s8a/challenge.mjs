import {loadS3ActorPack} from './actors.mjs';
import {S7_ROOMS,loadS7StockRoom,drawPreview} from '../flare-s71/rooms.mjs';
import {applyRunnerModel,runnerSummary,encounterCost,TRAP_IDS,SUPPORT_IDS} from '../flare-s71/game.mjs';
import {calibrateEncounter,estimateEncounter} from '../flare-s71/calibration.mjs';
import {startComposedHeroStance} from '../flare-s71/hero-preview.mjs';
import {decodeInviteCode,inviteCodeFromLocation,inviteSender} from './flow.mjs';
import {createReceiverSession,selectDungeon,setEncounter,advanceReceiver} from './receiver-session.mjs';
import {buildReceiverView} from './receiver-view.mjs';
import {buildCustomizationCatalog} from './catalog-view-model.mjs';
import {buildRoomCarousel} from './room-carousel.mjs';
import {roomSwipeDirection} from './swipe.mjs';
import {installRuntime} from './runtime-controller.mjs';

const $=id=>document.getElementById(id),specs=Object.values(S7_ROOMS),rooms=new Map();
let model,baseCatalog,catalog,invite,runner,session,calibrated,roomIndex=0,activePanel='monsters',runtimeStart=null;
const selectedSpec=()=>specs[roomIndex],selectedRoom=()=>rooms.get(selectedSpec().id);
const encounter=()=>({enemyTypes:[...document.querySelectorAll('[data-enemy-slot]')].map(x=>x.value),trapTypes:TRAP_IDS.filter(id=>document.querySelector(`[data-choice="${id}"]`)?.checked),supportTypes:SUPPORT_IDS.filter(id=>document.querySelector(`[data-choice="${id}"]`)?.checked)});
const usedBudget=e=>encounterCost(catalog,e);
const labels=(ids,kind)=>ids.filter(id=>id!=='none').map(id=>kind==='monster'?catalog.enemies[id]?.name:model.items?.[id]?.name||model.items?.[id]?.label||id);
function context(){const e=encounter(),estimate=estimateEncounter({catalog,model,runnerId:invite.runnerId,runner,encounter:e});return{runnerName:runner.name,runnerLevel:runner.level,roomName:selectedSpec().name,roomIndex,roomCount:specs.length,estimatedHpPercent:estimate.estimatedHpPercent,usedBudget:usedBudget(e),totalBudget:model.budget||100,activePanel,monsters:labels(e.enemyTypes,'monster'),traps:labels(e.trapTypes,'item'),supports:labels(e.supportTypes,'item')};}
function show(){const view=buildReceiverView({session,context:context()});for(const root of document.querySelectorAll('[data-screen]'))root.hidden=root.dataset.screen!==view.shell.screen;requestAnimationFrame(()=>$(view.shell.headingId)?.focus());renderCurrent(view);}
function renderCurrent(view){const m=view.model;if(view.kind==='invitation'){$('senderName').textContent=session.senderName;$('inviteRunner').textContent=`Level ${runner.level} ${runner.name}`;$('inviteTarget').textContent=`Target ${invite.targetHp}% HP`;}else if(view.kind==='mission'){$('missionTarget').textContent=`~${invite.targetHp}% HP`;$('missionRewardCue').textContent=m.rewardCue;$('targetFitCue').textContent=`${m.targetFit.label} · ${m.targetFit.note}`;renderRoom();}else if(view.kind==='customize'){$('dungeonBudget').textContent=m.budget.primary;$('dungeonBudget').dataset.legal=String(m.budget.legal);$('finishCustomize').disabled=!m.canFinish;renderTabs();}else if(view.kind==='ready'){$('readyTarget').textContent=m.title;$('readySummary').textContent=`${m.roomName} · ${m.budget.primary} · ${m.targetFit.label}`;$('runHero').disabled=!m.canRun;}}
function chooseSessionRoom(){session=selectDungeon(session,selectedSpec().id);session=setEncounter(session,encounter());}
function renderRoom(){const carousel=buildRoomCarousel({rooms:specs,index:roomIndex});$('roomName').textContent=carousel.current.name;$('roomCounter').textContent=carousel.counter;if(selectedRoom())drawPreview($('roomPreview'),selectedRoom().map,selectedRoom().tiles);}
function moveRoom(delta){roomIndex=(roomIndex+delta+specs.length)%specs.length;calibrated=calibrateEncounter({catalog,model,runnerId:invite.runnerId,runner,targetHp:invite.targetHp});applyEncounter(calibrated.encounter);if(session?.journey==='mission')show();}
function renderControls(){const vm=buildCustomizationCatalog({model,catalog,runnerId:invite.runnerId,runner}),monsterPanel=document.querySelector('[data-custom-panel="monsters"]');monsterPanel.replaceChildren();for(let i=0;i<3;i++){const label=document.createElement('label');label.textContent=`Guard ${i+1}`;const select=document.createElement('select');select.dataset.enemySlot=String(i);select.append(new Option('Empty · 0','none'));for(const item of vm.monsters)select.append(new Option(`${item.label} · ${item.cost}`,item.id));label.append(select);monsterPanel.append(label);}for(const kind of ['traps','supports']){const panel=document.querySelector(`[data-custom-panel="${kind}"]`);panel.replaceChildren();for(const item of vm[kind]){const label=document.createElement('label');const box=document.createElement('input');box.type='checkbox';box.dataset.choice=item.id;label.append(box,document.createTextNode(`${item.label} · ${item.cost} — ${item.summary}`));panel.append(label);}}for(const control of document.querySelectorAll('[data-enemy-slot],[data-choice]'))control.addEventListener('change',()=>{session=setEncounter(session,encounter());show();});}
function applyEncounter(e){[...document.querySelectorAll('[data-enemy-slot]')].forEach((x,i)=>x.value=e.enemyTypes[i]||'none');const selected=[...(e.trapTypes||[]),...(e.supportTypes||[])];for(const id of [...TRAP_IDS,...SUPPORT_IDS]){const box=document.querySelector(`[data-choice="${id}"]`);if(box)box.checked=selected.includes(id);}}
function renderTabs(){for(const tab of document.querySelectorAll('[data-custom-tab]')){const on=tab.dataset.customTab===activePanel;tab.setAttribute('aria-selected',String(on));document.querySelector(`[data-custom-panel="${tab.dataset.customTab}"]`).hidden=!on;}}
function transition(event){try{if(['USE_DUNGEON','CUSTOMIZE'].includes(event))chooseSessionRoom();if(event==='DONE')session=setEncounter(session,encounter());session=advanceReceiver(session,event);show();}catch(error){$('loadStatus').textContent=error.message;}}
$('acceptChallenge').addEventListener('click',()=>transition('ACCEPT'));
$('previousRoom').addEventListener('click',()=>moveRoom(-1));$('nextRoom').addEventListener('click',()=>moveRoom(1));
$('useDungeon').addEventListener('click',()=>transition('USE_DUNGEON'));$('openCustomize').addEventListener('click',()=>transition('CUSTOMIZE'));
$('finishCustomize').addEventListener('click',()=>transition('DONE'));$('editDungeon').addEventListener('click',()=>transition('CUSTOMIZE'));
$('resetSuggested').addEventListener('click',()=>{applyEncounter(calibrated.encounter);session=setEncounter(session,encounter());show();});
for(const tab of document.querySelectorAll('[data-custom-tab]'))tab.addEventListener('click',()=>{activePanel=tab.dataset.customTab;show();});
$('runHero').addEventListener('click',()=>runtimeStart?.());
let pointer=null;$('roomStage').addEventListener('pointerdown',e=>pointer={x:e.clientX,y:e.clientY});$('roomStage').addEventListener('pointerup',e=>{const direction=pointer&&roomSwipeDirection({startX:pointer.x,startY:pointer.y,endX:e.clientX,endY:e.clientY});pointer=null;if(direction)moveRoom(direction);});
window.__s8aReceiver={get session(){return session;},get encounter(){return encounter();},get roomId(){return selectedSpec().id;},get room(){return selectedRoom();},setRuntimeStart(fn){runtimeStart=fn;},startRuntime(){chooseSessionRoom();session=advanceReceiver(session,'RUN');show();},completeRuntime(result,score){session=advanceReceiver(session,'COMPLETE',{result,score});show();},replayRuntime(){session=advanceReceiver(session,'RUN_AGAIN');show();},runFailed(message){session={...session,journey:'ready'};show();$('readySummary').textContent=`Run could not start: ${message}`;},show};
installRuntime(window.__s8aReceiver);
(async()=>{try{const [base,game,heroPack]=await Promise.all([fetch('/quick-dungeon/flare-p0/data/catalog.json').then(r=>r.ok?r.json():Promise.reject(Error('Catalogue unavailable'))),fetch('/quick-dungeon/flare-s7/data/game.json').then(r=>r.ok?r.json():Promise.reject(Error('Game model unavailable'))),loadS3ActorPack()]);baseCatalog=base;model=game;const code=inviteCodeFromLocation(location);invite=code?decodeInviteCode(code,model):new URLSearchParams(location.search).get('demo')==='1'?{runnerId:model.defaultRunner,targetHp:model.defaultTargetHp}:null;if(!invite)throw Error('Challenge invitation is missing or invalid');catalog=applyRunnerModel(baseCatalog,model,invite.runnerId);runner=runnerSummary(model,invite.runnerId,catalog);session=createReceiverSession({invite,senderName:inviteSender(location.search)});renderControls();calibrated=calibrateEncounter({catalog,model,runnerId:invite.runnerId,runner,targetHp:invite.targetHp});applyEncounter(calibrated.encounter);session=setEncounter(session,encounter());startComposedHeroStance($('inviteHeroCanvas'),heroPack);await Promise.all(specs.map(async spec=>rooms.set(spec.id,await loadS7StockRoom(spec.id))));$('acceptChallenge').disabled=false;$('loadStatus').textContent='Your Hero-Runner is ready.';show();window.__s8aData={model,catalog,invite,runner,rooms,specs};}catch(error){console.error(error);$('heading-invitation').textContent='THIS CHALLENGE CANNOT OPEN';$('loadStatus').textContent=error.message;}})();
